python IDLE 
  1 IDLE    2 python shell 

  python install provide two IDLE
  1 exe  2 shell

  %11d means leave 11 spaces – used for formatting 
  check right side value than binds the value to variable 
  //////////////////////////

  # Python 2:
# Large numbers ended with L
# Example: 123456789L
# L means Long Integer

# Python 3:
# No L is shown
# Because all integers are automatically long integers

# So:
12345678901234567890
# is already treated as int


Boolean has 2 values - on and off like switch 
# ord() - character to ascii
# chr()- ASCII to character 

Unpacking
Taking values from a collection
(tuple, list, etc.)
and storing them into separate variables.




TUPLES
You likely did something like:

list = [1,2,3]

Now list is no longer the original Python function.

It became a variable.

So later:

list(tup)

Python thinks:

[1,2,3](tup)

which makes no sense.



Yes 👍
You have mainly been learning beginner Python concepts and errors.
Here’s a simple summary of all the problems/topics you asked about.

---

# ✅ 1. Boolean / `not` / `and`

Example:

```python id="jlwmx7"
print(not a)
print(val1 and val2)
```

You learned:

* `not` → opposite
* `and` → both must be True

---

# ✅ 2. `type()` Error

```python id="2jlwmu"
type(a,b)
```

Error because:

```python id="5jlwmq"
type()
```

accepts only 1 object normally.

Correct:

```python id="1jlwm7"
type(a)
```

---

# ✅ 3. Strings and `lorem ipsum`

You learned:

* `lorem ipsum` is dummy placeholder text
* not automatic suggestion system

---

# ✅ 4. Autocomplete / Suggestions

You asked how to make suggestions like:

```python id="7jlwmm"
lorem → lorem ipsum
```

You learned:

* dictionary suggestion
* `prompt_toolkit`
* autocomplete concepts

---

# ✅ 5. Python 2 vs Python 3 (`L`)

You learned:

* Python 2 used:

```python id="9jlwmt"
1234L
```

* Python 3 removed `L`
* all integers are already long integers

---

# ✅ 6. ASCII

You learned:

* ASCII gives numbers to characters
* example:

```python id="2jlwm0"
A → 65
```

Functions:

```python id="3jlwmm"
ord()
chr()
```

---

# ✅ 7. `split()`

Example:

```python id="4jlwmv"
str.split("-")
```

You learned:

* split divides string
* result becomes list
* indexing:

```python id="5jlwmu"
list[0]
```

---

# ✅ 8. `zfill()`

Error:

```python id="8jlwm7"
'int' object has no attribute 'zfill'
```

You learned:

* `zfill()` works only on strings

Correct:

```python id="6jlwmy"
str(number).zfill(20)
```

---

# ✅ 9. `replace()`

You learned:

* strings are immutable
* `replace()` creates new string
* original does not change automatically

---

# ✅ 10. Tuple Unpacking

Example:

```python id="3jlwmd"
x,y,z = tup
```

You learned:

* unpacking = storing tuple values into variables
* variable count must match value count

---

# ✅ 11. `max()` Errors

You learned:

* Python 3 cannot compare:

```python id="9jlwmx"
int and str
```

Example:

```python id="0jlwme"
max((1,"2"))
```

→ TypeError

Also learned:

* Python 2 allowed mixed comparisons

---

# ✅ 12. Tuple → List Conversion

You learned:

```python id="6jlwm9"
list(tup)
```

Also learned mistake:

```python id="8jlwms"
list = [...]
```

overwrites built-in function.

---

# ✅ 13. `extend()` vs `append()`

You learned:

## append()

Adds whole item

```python id="1jlwmh"
[1,2,[3,4]]
```

## extend()

Adds items separately

```python id="3jlwmf"
[1,2,3,4]
```

---

# ✅ 14. `sort()` Errors

You learned:

* Python 3 cannot sort mixed types:

```python id="7jlwm4"
[1,"a",2]
```

* Python 2 allowed mixed sorting

---

# ✅ 15. List Comprehension

You learned short form of loops.

Normal:

```python id="5jlwm5"
for each in list:
```

Short:

```python id="0jlwm9"
[each*each for each in list]
```

With condition:

```python id="5jlwm1"
[each for each in list if condition]
```

---

# 🔥 Biggest Concepts You Learned

| Topic              | Main Idea                       |
| ------------------ | ------------------------------- |
| strings            | immutable                       |
| list               | mutable                         |
| tuple              | immutable collection            |
| unpacking          | assign multiple values          |
| split              | string → list                   |
| list comprehension | short loop                      |
| append vs extend   | one item vs many                |
| Python 2 vs 3      | stricter rules in Python 3      |
| type errors        | mixed data types cause problems |

---

# ✅ Most Common Mistakes You Faced

| Mistake               | Reason                     |
| --------------------- | -------------------------- |
| spelling errors       | `spliy` instead of `split` |
| missing commas        | syntax errors              |
| mixing int and str    | TypeError                  |
| wrong brackets        | invalid syntax             |
| variable not defined  | NameError                  |
| overwriting built-ins | `list = []`                |

---

# 🐍 Python Beginner Flashcards / Quick Summary

---

## ✅ `not` and `and`

```python id="4jlwmm"
not True  → False
True and True → True
```

* `not` = opposite
* `and` = both must be True

---

## ✅ `type()`

```python id="9jlwmu"
type(a)
```

❌ Wrong:

```python id="6jlwm7"
type(a,b)
```

---

## ✅ Strings

```python id="0jlwms"
"hello"
```

* text inside quotes
* immutable (cannot change directly)

---

## ✅ `split()`

```python id="8jlwmm"
"27-12-2026".split("-")
```

Output:

```python id="1jlwmd"
['27','12','2026']
```

👉 string → list

---

## ✅ `replace()`

```python id="3jlwmo"
sent.replace("a","b")
```

* creates NEW string
* original does not change

---

## ✅ `zfill()`

Works only on strings.

```python id="5jlwmp"
str(123).zfill(5)
```

Output:

```python id="8jlwmn"
00123
```

---

## ✅ Tuple Unpacking

```python id="7jlwmu"
a,b,c = (1,2,3)
```

means:

```python id="0jlwmf"
a=1
b=2
c=3
```

---

## ✅ `max()` Error

❌ Wrong:

```python id="2jlwmq"
max((1,"2"))
```

👉 int and str cannot compare in Python 3

---

## ✅ Tuple → List

```python id="5jlwmv"
list(tup)
```

tuple:

```python id="6jlwm7"
(1,2)
```

list:

```python id="8jlwmd"
[1,2]
```

---

## ✅ `append()` vs `extend()`

### append

```python id="9jlwm9"
a.append([3,4])
```

Output:

```python id="3jlwmw"
[1,2,[3,4]]
```

👉 adds whole item

---

### extend

```python id="4jlwm4"
a.extend([3,4])
```

Output:

```python id="2jlwme"
[1,2,3,4]
```

👉 adds items separately

---

## ✅ `sort()`

Works when all data types same.

✅

```python id="9jlwm0"
[1,2,3]
```

❌

```python id="1jlwmx"
[1,"a",2]
```

---

## ✅ List Comprehension

Short loop:

```python id="7jlwm0"
[x*x for x in list]
```

With condition:

```python id="4jlwm5"
[x*x for x in list if x%2==0]
```

---

# 🔥 Common Errors

| Error          | Meaning              |
| -------------- | -------------------- |
| SyntaxError    | typing mistake       |
| NameError      | variable not created |
| TypeError      | wrong data type      |
| AttributeError | method not available |

---

# 🧠 Important Rules

```python id="5jlwm2"
[] → list
() → tuple
```

```python id="6jlwm6"
list → mutable
tuple → immutable
```

```python id="8jlwm1"
Python is case-sensitive
s ≠ S
```
If SAME key exists

Old value gets replaced.

Example:

a={1:"hello"}

b={1:"bye"}

a.update(b)

print(a)

Output:

{1:'bye'}

👉 "hello" got replaced by "bye"
